package com.example.LifeMaster_BE.UserManager.Peristalsis.Google.Login;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.util.Optional;

@Repository
public interface GoogleRepo extends JpaRepository<GoogleUserEntity, Long> {
    Optional<GoogleUserEntity> findByGoogleId(String googleId);  // 구글 ID로 사용자 조회
    Optional<GoogleUserEntity> findByEmail(String email);  // 이메일로 사용자 조회
}

 */


