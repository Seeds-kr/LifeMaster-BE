package com.example.LifeMaster_BE.UserManager.Peristalsis.Google.Login;
/*
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.oauth2.core.oidc.user.OidcUser;
import org.springframework.security.oauth2.core.user.OAuth2User;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

    @RestController("/googleLogin")
    public class GoogleLoginController {

        @GetMapping("/user")
        public String userInfo(@AuthenticationPrincipal OAuth2User user) {
            return "User Info: " + user.getAttributes();
        }
        @GetMapping("/home")
        public String home() {
            return "Welcome to Home Page!";
        }

        @GetMapping("/login")
        public String loginPage() {
            return "login.html"; // login.html을 반환
        }
    }


 */

